<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/login_activity_navigation"
    app:startDestination="@id/login_fragment">

    <fragment
        android:id="@+id/register_fragment"
        android:name="solutus.coronawatch.ui.loginActivity.login.RegisterFragment"
        tools:layout="@layout/register_fragment">
        <action
            android:id="@+id/to_login_fragment_action"
            app:destination="@+id/login_fragment" />

    </fragment>
    <fragment
        android:id="@+id/login_fragment"
        android:name="solutus.coronawatch.ui.loginActivity.login.LoginFragment"
        tools:layout="@layout/login_fragment">
        <action
            android:id="@+id/to_register_fragment_action"
            app:destination="@+id/register_fragment" />

    </fragment>


</navigation>